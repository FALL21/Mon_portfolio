# Copié sur le VPS en /opt/portfolio/.env (ou export avant compose).
# Bind local-only : Nginx (80/443) proxifie vers 127.0.0.1:3010.
# Ne pas exposer 3010 sur Internet.
PORTFOLIO_PUBLISH=127.0.0.1:3010
